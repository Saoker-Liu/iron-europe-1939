# 开发说明 · 1939地理重建版

先阅读 MAP_NOTES.md 了解时间口径、历史校订、地理尺度和数据许可。

## 架构

- `index.html`：离线入口、样式、图例。
- `js/loader.js`：顺序加载各模块，无网络请求依赖。
- `js/core/hex.js`：odd-r六边形距离。
- `js/core/geography.js`：所有地理数据共享的兰伯特等角圆锥投影，格心45公里。
- `build_map.js`：读取本地GeoJSON，生成地图、1939母国层、城市格、部署、海峡阻断边、河流与登陆候选格。
- `map-placement.js`：构建时使用的带禁配约束的最小代价格位分配器；浏览器不加载。
- `map_sources/`：原始数据、来源清单、SHA-256、许可证。
- `js/data/map.js`：生成文件，不手改。
- `js/data/nations.js`：城市经纬度、国家、首都与占领区关系。
- `js/data/terrain.js`：地形通行/防御规则；不再保存另一份旧地图。
- `js/core/assemble.js`：装配生成地图和游戏数据；浏览器与Node共享。
- `js/engine/game.js`：移动、河流、运输、战斗、固定国界内的控制权、经济、AI、事件、存档。
- `js/ui/ui.js`：缓存渲染、边界、河流、登船面板、政区/地形切换、全览、交互。

## 坐标和地图修改

1. 城市只维护WGS84 `lon/lat`，不要把旧版 `col=lon*2+24` 写入新逻辑。
2. 生成器先保留历史锚点，再按国家统筹分配独立陆地格；港口须邻海，候选格距离小于60公里。
3. 旧部队部署坐标只在生成器中作为旧版部署的地理参考，输出约束到本国陆地，避免开局在中立国或海上。
4. 母国 `HOME_COUNTRIES` 不随占城变化。`territoryOwner` 只在同一国的城市影响范围内查询控制者。
5. `neighbors` 为几何邻接；陆路必须用 `landNeighbors` 或显式检查 `blockedEdges`。
6. 海运使用相邻海洋格，不能添加固定传送边或海峡陆桥。AI距离场需要海陆转换代价。
7. 河流路径使用投影后的连续网格横坐标，不再额外添加odd-r偏移。
8. 新版存档v3必须携带 `mapVersion`，旧存档要明确拒绝，不能按城市数组下标盲目恢复。

## 验证

执行 `node build_map.js --write` 后，运行 `node build_map.js --check`、`node test_placement.js`、`node test_geography.js`、`node test_render.js` 和 `node test_logic.js`。

连通性测试必须同时包含“应当连通”和“应当隔海”，不能再以所有城市可徒步到达作为正确条件。保持数据来源记录；新历史年份应创建独立快照，不能只改地图标题。

地图纹理仍用缓存层；缩放和政区模式切换会重建缓存。正常渲染不应逐帧搜索地理多边形或重建整张地图。

## 渲染时序回归

移动动画以performance.now()记录开始时间，而requestAnimationFrame参数为帧时间；第一帧可能早于动画开始。原代码将负时差转换为路径索引-1，触发TypeError，且因下一帧在函数末尾预约，错误后绘制循环永久停止。现将经过时间限制为非负数，起点保存为格坐标以跟随相机，所有临时特效进度限制在0—1；渲染入口通过finally保证预约下一帧，同时让异常继续上报。

地形缓存比较与生成使用同一个受限分辨率，避免相机缩放大于缓存上限时每140毫秒重建整图；缓存按地图尺寸限制在约1200万像素，绘制成功后才写入有效键。test_render.js通过生产UI代码、可控帧时间和最小Canvas/DOM适配器覆盖首次移动、定时器延迟、拖动、特效、缓存失效和单帧异常恢复。该测试不替代浏览器像素验收。

## 城市扩充

当前地图版本europe-1939-geographic-v8，共436处城市与基地；序列化结构现为v4（读取旧v3陆上存档），地图版本独立校验。城市的note、region、major分别用于历史说明、地区搜索和中等缩放下的重点标签。UI新增城市搜索与任意城市的信息面板，只有己方空城显示招募选项。城市坐标生成保持独立格位和小于60公里的检查上限；敦刻尔克、扎拉及新加入的小岛／阜姆作为显式历史边界锚点。新增7处节点另有精确落格、海运连通、无陆桥及柯尼斯堡苏联飞地回归检查，详见MAP_NOTES.md。

经济文件的countryIncomeBudget记录扩充前国家总收入，各城市的分配值总和必须与其一致。城市扩充后须重新生成地图、更新静态图与离线ZIP，而非仅修改显示名称。


## v6 地区节点与海岸拓扑

新增66处节点，国家总收入预算不变。nations中的mapLabel将岛名与港口标记绑定，port要求城市落在邻海格，demilitarized禁止玩家及AI通过recruit在奥兰招募；不模拟完整条约、驻军和战时例外。build_map保留小岛专用格位，避免其它城市先占用；修复康沃尔窄颈与布里斯托尔湾水面，并阻断塞文河口直接步行。小岛不使用脱离实际节点的自由浮动地区标签。

test_geography增加全部新增地区的1939母国检查、三座海岛隔水与海运、博恩霍尔姆标签绑定、康沃尔陆路、港口水面连通大西洋、普利茅斯落格、奥兰招募与存档状态检查。城市落格偏移仍要求小于80公里，本次最大约41公里。发布流程的version.json从实际地图数据读取版本和节点数量。


## v7 基尔运河与51处新增地点

MAP_META.canals保存人工水道的经纬度折线、投影路径、引线锚点、历史名称及两端城市。UI在政区／地形两模式的缓存中绘制独立青色线及标签，导出脚本使用同一数据。运河不写入海洋格、blockedEdges或riverEdges；当前桥梁与渡口由常规陆路移动抽象，不引入未实现的海军机制。build_map.js的手工概化折线依据见MAP_NOTES.md。

库雷萨雷保留真实位置锚点与岛名绑定，并添加塔林海运。新增城市仍遵守母国、独立格位、港口岸线及位置偏移约束；苏联共有64处城市，使用1939年地名。存档槽v7，序列化结构仍为v3。

test_geography增加基尔运河两端与长度范围、路径投影、北德陆路保持、萨列马岛隔水与海运、刻赤海峡绕行、七个指定苏联城市与波罗的海三国归属、两座诺夫哥罗德区分以及v6旧存档拒绝。test_render检查生产渲染代码确实画出运河名称与萨列马岛标签。

## v8 地区加密与城市格位分配

新增107处节点，总计436处。城市数据记录1939归属、经纬度、region和历史说明；国家收入总预算不变。地图与独立存档槽为v8，拒绝v7及更早地图存档，序列化结构仍为v3。

原来的逐城贪心落格会将后处理的相邻城市挤向远处。本版先固定小岛／边界锚点，再以城市和合法陆地格为二分图，按国家求最小总平方距离的分配；候选距离小于60公里，海港要求邻海，禁止重复格位。代价取整数避免跨平台浮点微差影响可复现结果。新增日内瓦、海牙、欧登塞和马斯特里赫特锚点；南林堡向城市北侧概化一格以保持与荷兰相连、避免覆盖列日。详见MAP_NOTES.md。

`test_placement.js`覆盖贪心失败案例、禁配约束、无解和80组小矩阵的穷举最优解对照；`test_geography.js`检查全部24个指定城市、城市独立格位、海牙与乌德勒支方位、首都标记、日内瓦与南林堡、欧登塞隔水和渡运，以及波兰东部1939归属。墨西拿海峡的新渡运仍不允许直接步行。所有436处节点的最大偏移约41公里。离线构建包必须包含map-placement.js。

## 分级地图文字

`map-label-data.js`维护国家、海域、岛屿和地区的WGS84锚点及独立显示层级，并明确城市文字等级，不能使用几乎所有新增城市都有的major字段作为唯一筛选依据。build_map将labels、cityLabelLevels、labelZooms和labelRevision写入MAP_META。数据修订仅涉及展示元数据，地图版本与v8存档槽不变。

相机倍率z的分界为0.24、0.65、1.3（最大倍率仍为2.2），分别为全览、地区、局部、全部城市。地区文字各层互斥；城市按cityLabelLevels <= level累加，1.3起显示全部城市，距最大倍率约log(2.2/1.3)/log(1.12)=4.64次放大滚轮。`js/ui/map-labels.js`是浏览器与Node共用的纯选择／布局函数，使用字体实际测量宽度进行屏幕坐标避让，只处理视野内锚点；所有达到显示级别的城市名都不因重叠而丢弃；先排主要城市，再排次级与其它城市，最后安排地区文字。长城市名可换行，移动较远的标签使用引线指回真实锚点。

文字从地形缓存中移出，由drawMapLabels每帧绘制，文字布局仅在相机、视口或游戏对象改变时重算。国家名在政区与地形模式遵循相同层级。运河水道仍在地形缓存，名称在局部层；城市图形与旗帜仍在缓存，名称单独绘制。切换文字层级不触发全地图纹理重建，不能再将受限的terrainScale用于判断标签等级。

`test_labels.js`检查全部指定地名、层级边界、地区父级退出、城市集合随缩放单调增加、436座城市提前完整覆盖、坐标投影、岛屿锚点和碰撞策略；test_render用生产UI检查镜头缩放、层级切换和纹理复用。导出脚本复用MapLabels的选择／布局，生成两张全览图及label-levels.png六视角对照；Pillow字体测量和栅格化不等于浏览器交互验收。

## 运输舰艇机制（存档结构v4，地图仍为v8）

ECONOMY.transports定义三档价格、年份、固定移动力、海上防御和攻击比例。unit.transport保存装备id，unit.embarked表示海上状态；transportOf、isEmbarked与movementEndsTurn由引擎和UI共享。购买限制为未行动的沿海陆军，升级差价仅在陆上支付；装备上岸后保留。moveUnit重新验证合法路径，移除了旧的非法目的地兜底和调用者自定义路径绕过。

moveRange在陆上只扩展陆地，在海上只扩展海洋；仅从起点追加相邻上下船格，并将海陆转换标为整回合动作。移动接敌预览跳过海陆转换目的地，攻击、反击共享canStrikeFrom。运输炮兵射程固定1；船防直接替换陆军防御，运输状态不自动补员、不显示驻防按钮。海上棋子和面板显示舰艇及受罚属性。

build_map不再生成routes，ferryDestinations接口及UI虚线已删除。transportNeighbors只用于战略连通与AI距离场，不是可绕过行动限制的移动API。AI距离场覆盖海洋，以5的转换代价和1的海格代价寻找海路；岸边单位在海路更优时购买已解锁、可负担的舰艇，再使用普通moveUnit行动。

存档v4记录运输装备和状态，旧v3缺省为未配船／未登船。读取时拒绝未知舰艇id、空军海运、海上未登船或陆上登船等不一致状态。test_transport覆盖购买／科技／升级、路径不可绕过、上下船结束行动、固定速度与防御、海上攻击与反击、保存迁移及AI真实渡海占城。test_logic的单位地形不变量改为陆上单位必须在可行陆地、登船单位必须在海洋。test_render验证购买按钮、登船面板及海上驻防禁用。

## 独立海军（存档v5）

naval.js提供八舰种、国家舰型、39处军港候选和三处亚格网海峡。assemble合并独立的装备字典与完整克制矩阵；陆军rosterFor不变。Game.buildHarbors按稀缺泊位优先确定互不重叠的邻海格。recruitNaval每次复核控制权、当前最新型号、经济、科技与泊位，不接受旧舰型或陆军装备。占领军港使用占领阵营主要国家舰型，避免生产出敌阵营单位。

isSeagoing同时涵盖独立海军与运输陆军；海军不触发上下船。seaNeighbors只额外连接真实海峡两端，按hexDist计费。seaDistances为带权水面距离场，AI不能越陆追敌。海军AI遍历所有己方空闲军港，按实时余额建造；不设置预算比例、每回合数量、舰队规模或军港数量衍生的舰队上限，和平与战争使用相同生产条件。新舰当回合锁定行动，维修只发生于己方军港。

canStrikeFrom统一目标合法性，canCounter统一实际反击与AI预估。潜艇目标必须在海上；反潜仅限潜艇、驱逐舰、航母与空军。海军存档要求海格且没有陆军运输状态；旧河口陆军存档迁移至原地运输状态。test_naval和test_render覆盖引擎与真实UI按钮路径，CI均执行。

## 舰名与主剧本舰队（存档v6）

naval-names.js按现有eqKey配置名称池，保持装备键和科技顺序稳定。单位ct、eq.cls、eq.className、shipName分别表示国家、舰种、舰级和舰名；unitName用于单位标题／日志，navalIdentity提供完整身份。名称元信息由受信任的池数据派生，不接受存档自由HTML。usedShipNames记录国家内已用舰名与改装舰体标识；沉没不释放。nextShipName只预览，spawnUnit才登记；编号按国家／舰种跨级连续。

naval-scenario.js单独定义主剧本91艘舰队，Game在陆空部署后由基地进行水路搜索，避开占用格和全部军港泊位。不会修改生成地图中的陆空deployments，也不设置舰队数量上限。美国舰队开局中立，usa事件将cf.us改为west，现有舰艇随之激活。其它剧本可使用initialFleet:false关闭主剧本注入，再提供自身编制。

deserialize以initialFleet:false构造，再恢复原单位；两遍处理舰名，先保留已有有效姓名、拒绝重名或不合法姓名，再按单位id给旧无名舰艇补名。v6保存已用名清单；旧存档已沉没舰艇的名字无法追溯。test_naval_names覆盖历史池／通用编号／失败建造不占名／同舰体改装别名／沉没与存档／旧档不补舰／91舰位置、1939解锁、泊位空闲和美国参战。UI测试验证四项身份、建造预览、悬停和放大后的地图舰名。


### v8海岸校正存档兼容

地图及存档新增 `terrainRevision: 1`，保留地图版本v8和原有城市格。无该字段的旧存档仅针对9个本次变更格检查地形适用性；不适用的单位迁至4格内最近空闲陆地／海洋，保留单位属性并写入日志。所有原位置及已分配位置预留以防叠放，无可用位置则明确报错。新存档中非法位置仍拒绝读取。回归覆盖连续岛屿、海湾真实邻接通海、湖泊类型、乌拉尔河几何、陆军／舰船／运输状态迁移及重复读档。


### v7机场空军

`Game.airfields`按首都／主要城市层级生成机场（93处，排除非军事区）。空军继续参与统一的兵力、将领和战斗序列，但用`airbase`指向机场，`c/r`必须等于机场坐标；`unitAt`仅查询地面／海上层，不再用机场飞机阻挡陆军。界面从机场清单选择空军，地图不重复画叠在城内的飞机棋子。

`airRoster / airBuildError / recruitAir`独立校验机场、所有权、经济、国家装备和年份；普通`recruit / rosterFor`禁止空军。`airRadius`使用原移动力与适用将领加成，`canStrikeFrom`只从所属机场计算半径，`moveRange / moveUnit`仅表示耗尽回合的己方机场转场，`computeTargets`不得将转场纳入移动后攻击。飞机不能占城、驻防、运输。占城及首都全境易帜后清理失去基地的飞机并释放将领。

旧存档v3—v6飞机自动归入最近己方机场；v7严格校验机场位置和所属阵营。军港、运输及地面移动不变。`test_air.js`和UI回归覆盖机场入口、购买、分层、半径、出击、转场、被占领、读档及AI；战役模拟将“地面不叠格”和“飞机在有效基地”分别作为不变量。


### v8航空装备与特殊任务

`js/data/air.js`独立配置七个`airRole`、七国及通用装备树、伞兵、核打击参数。引擎仍以`cls: air`统一将领／机场接口，装备克制由角色专用倍率计算；运输机在攻击资格与伤害计算两层拒绝攻击。旧:air:0编号保留原型对应，后续不重排已发布编号。

伞兵采用`cls: inf, para: true`，装备键为`国家:para:0`，仅机场城市招募。`carrierId`关联运输机，载员保留在单位表但退出地图占位、主动行动及可攻击目标；载员随转场、卸载和伞降更新坐标，运输机被销毁时递归清理载员和将领。读取存档校验一机一支伞兵、阵营、坐标和有效机型，拒绝孤立关联。

`fallout`保存格键到1—6的剩余结算次数。`cityIncome`统一污染收入；完整回合末对所有阵营执行一次污染伤害，在玩家收入结算后减少剩余次数，以保证六次伤害和六次收入抑制。`startTurnFor`污染格停止补员。重叠打击刷新时长，不叠加每回合伤害。核爆命中所有单位层，包括机场飞机和载员，波及非交战阵营会宣战；不会改变城市占领权。界面核打击单独确认，空白格也可选择；引擎再次检查年份、行动、经济、半径，避免过期按钮绕过限制。

`test_air_equipment.js`验证角色、升级、载运、伞降占城、核爆全层毁伤、六回合伤害和收入恢复、存档非法数据；`test_render.js`验证真实装载／伞降／核打击取消与确认按钮路径及污染显示。AI可选用航空装备、载运机场伞兵，并仅在足够资金、敌军集中且无己方受波及时尝试核打击。
