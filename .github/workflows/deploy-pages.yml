name: 部署游戏到 GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: 检查地图与游戏逻辑
        run: |
          find js -name '*.js' -print0 | xargs -0 -n1 node --check
          node build_map.js --check
          node test_placement.js
          node test_geography.js
          node test_labels.js
          node test_render.js
          node test_tutorial.js
          node test_unit_icons.js
          node test_i18n.js
          node test_i18n_landing.js
          node test_i18n_campaign.js
          node test_music.js
          node test_generals.js
          node build_general_catalog.js --check
          node test_diplomacy.js
          node test_undo_movement.js
          node test_unit_management.js
          node test_minimum_movement.js
          node test_transport.js
          node test_air.js
          node test_air_equipment.js
          node test_air_scenario.js
          node test_construction.js
          node test_infantry.js
          node test_artillery.js
          node test_armor.js
          node test_ground_scenario.js
          node test_naval.js
          node test_naval_names.js
          node test_logic.js
      - name: 打包游戏和地图资料
        run: |
          mkdir -p _site/artifacts
          cp survey.html README.en.md DIPLOMACY.en.md DIPLOMACY.md GENERAL_CATALOG.md MUSIC.md index.html README.md MAP_NOTES.md CITY_CATALOG.md NAVAL_CATALOG.md NAVAL_NAMES.md AIR_CATALOG.md _site/
          cp -R js map_sources music docs landing tools _site/
          cp artifacts/europe-1939-political.png artifacts/europe-1939-terrain.png artifacts/label-levels.png artifacts/naval-ports-detail.png artifacts/coast-river-detail.png _site/artifacts/
          touch _site/.nojekyll
          node -e "const fs=require('fs'),p='_site/index.html';fs.writeFileSync(p,fs.readFileSync(p,'utf8').replace(/js\/loader\.js\?v=[^\"]+/,'js/loader.js?v='+process.env.GITHUB_SHA));"
          node -e "const fs=require('fs'),D=require('./js/data/load-node');fs.writeFileSync('_site/version.json',JSON.stringify({commit:process.env.GITHUB_SHA,mapVersion:D.MAP_META.version,cities:D.CITIES.length})+'\n');"
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: 确认仓库已启用 Pages
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if ! gh api "repos/$GITHUB_REPOSITORY/pages" --silent; then
            echo "::error::GitHub Pages 尚未启用或不可访问。请仓库管理员在 Settings > Pages 将 Source 设为 GitHub Actions，然后重新运行本工作流。"
            echo "部署未完成：需要仓库管理员启用 [GitHub Pages](https://github.com/$GITHUB_REPOSITORY/settings/pages)。私有仓库需支持私有仓库 Pages 的账户套餐。游戏检查与站点打包已完成。" >> "$GITHUB_STEP_SUMMARY"
            exit 1
          fi
      - uses: actions/configure-pages@v5
      - id: deployment
        uses: actions/deploy-pages@v4
