# 开发与验证

## 目录用途

- `index.html`、`landing/`：游戏入口、介绍页和当前版本实机截图。
- `js/loader.js`：按顺序加载本地模块，版本参数同步传递给子脚本；无游戏运行依赖。
- `js/data/`：国家、城市、装备、经济、将领、初始部署与事件配置。
- `js/core/`：odd-r六边形、地理投影和共享数据装配。
- `js/engine/game.js`：行动、战斗、设施、AI、经济与存档；不依赖DOM。
- `js/engine/diplomacy.js`：国家控制权、最近城市依附、局部战争、割让、吞并与特别和约。
- `js/engine/tutorial.js`：独立演习场景，不读写正式战役存档。
- `js/ui/`：真实交互、帮助、音乐、矢量图标和分层标签。
- `map_sources/`：构建所需GeoJSON、来源清单和许可证，不能作为临时文件删除。
- `build_map.js`、`map-placement.js`、`map-label-data.js`：地图生成、城市格位分配与文字锚点；生成的 `js/data/map.js` 不手改。
- `test_*.js`：维护中的回归测试；发布前由工作流执行，不是开发垃圾。
- `artifacts/render-map.py`：从当前地图生成可导出的PNG，需要Python/Pillow与中文字体；`tools/icon-preview.html` 用于图标检查。

## 修改约束

城市维护WGS84经纬度；构建器投影到128×112格，格心45公里。`HOME_COUNTRIES`只是开局底图，条约通过`landTransfers`改变法律归属；`territoryCountry`读取全体最近城市的共同控制国。地图缓存须包含国家控制权及政治修订号，不能只按阵营首字母区分。

陆海通行必须检查海峡禁行边和湖泊。等代价寻路以中立领土风险作次级排序。玩家行动的宣战确认在UI中完成，AI无需弹窗；移动接敌要合并确认路径与目标国。引擎再次验证合法行动，不能信任UI传入的路径。

空军使用`airbase`，可与陆军同格；`unitAt`不含飞机和运输机载员。`carrierId`表示伞兵载员，运输机损失会同时移除载员。军事数据的既有装备键须保持稳定，兼容旧存档。

`movementState`记录撤销比对快照，`pureMovement`只允许位置和行动状态变化。占领、宣战、事件及其他单位变化必须使撤销失效；撤销仅恢复移动单位及其随行载员，不重建军队或回滚外交。旧的非纯移动撤销记录不会恢复。

当前存档结构v9，携带`mapVersion`、`terrainRevision: 2`、设施、部队、将领、外交与污染等。构造读档对象时不重复注入开局部队；仅对明确校正的旧海岸格执行迁移，不把任意非法坐标自动修正。教程存档与主战役隔离。

地形、国界、河流使用缓存；每帧不得重建全图几何。单位和动态文字做视口剔除；Path2D复用，图标描边与填充共用变换。渲染动画时间戳须容忍首帧早于动画起始时刻。DOM/Canvas适配测试不能代替浏览器截图检查。

## 检查与生成

修改地图：`node build_map.js --write`，随后 `node build_map.js --check`。

修改将领：更新数据后运行 `node build_general_catalog.js`（支持 `--check` 验证目录同步）。

完整检查列表维护在 `.github/workflows/deploy-pages.yml`。重点包括地图与格位、渲染与真实按钮交互、教程、外交、纯移动撤销、中立宣战确认、陆海空军、设施、初始部署和最多80回合的三阵营模拟。模拟检查非法地形、重叠、负经济和异常，不等于游戏平衡验收。

文案、截图或链接修改至少验证相关UI、介绍页资源链接及浏览器窄屏布局。不要把一次历史测试结果写成永久有效的验收承诺；每次发布以对应Actions记录为准。

## 发布

主分支为 `main`，推送后自动检查、打包并部署GitHub Pages；站点 `version.json` 记录实际提交。必须检查工作流结果和线上版本，不能将“已推送”等同于“部署成功”。标签 `v*` 触发离线包Release。已生成地图与本地音乐允许直接打开 `index.html`，不需要本地服务器。

运行数据、来源与许可证、测试、构建器和当前功能说明均应保留。一次性调试脚本、旧测试报告、被替换的截图和临时日志不应进入发布包。删除文件后同时更新工作流与所有相对链接。

## 双语与兵模

`js/ui/i18n*.js`提供中文／英语显示；介绍页和游戏都支持切换。`?lang=en|zh`优先于本地语言偏好，用于跨离线页面传递语言。新增提示必须使用T/F并补词条，新增数据需通过英语数据审计。`i18n-en-updates.js`维护当前规则与事件的增补，不能用旧分支文案覆盖新机制。

逻辑标识保持稳定：招募分组、装备键、城市键和舰名存档标识不翻译；舰名通过displayShipName展示英语。切换语言前等待行动结束并保存正式战役。战报保存模板与参数，旧战报通过已知模板显示翻译。test_i18n_campaign.js检验跨语言存档、舰名唯一性和现行外交数据。

兵模由UnitIcons统一用于Canvas和SVG，包含陆军细分类别、7种空军、8种海军及运输船侧影。
